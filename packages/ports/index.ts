/**
 * @fileoverview Public entry point of the external ports: interfaces only for
 * the six external systems (ADR-0001). Real adapters live in their own
 * packages; fakes live behind the separate `testing.ts` entry point.
 */

export {};
